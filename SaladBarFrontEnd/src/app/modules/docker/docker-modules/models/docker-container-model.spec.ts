import { DockerContainerModel } from './docker-container-model';

describe('DockerContainerModel', () => {
  it('should create an instance', () => {
    expect(new DockerContainerModel()).toBeTruthy();
  });
});
