import { DockerNetworkModel } from './docker-network-model';

describe('DockerNetworkModel', () => {
  it('should create an instance', () => {
    expect(new DockerNetworkModel()).toBeTruthy();
  });
});
