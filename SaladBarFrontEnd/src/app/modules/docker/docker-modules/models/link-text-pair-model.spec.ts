import { LinkTextPairModel } from './link-text-pair-model';

describe('LinkTextPairModel', () => {
  it('should create an instance', () => {
    expect(new LinkTextPairModel()).toBeTruthy();
  });
});
