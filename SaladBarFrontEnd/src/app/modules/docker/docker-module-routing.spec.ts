import { DockerModuleRouting } from './docker-module-routing';

describe('DockerModuleRouting', () => {
  it('should create an instance', () => {
    expect(new DockerModuleRouting()).toBeTruthy();
  });
});
